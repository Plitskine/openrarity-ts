{
  "name": "openrarity-ts",
  "version": "1.0.1",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "prepublish": "npm run build",
    "watch": "tsc -p tsconfig-build.json -w",
    "build": "tsc -p tsconfig-build.json",
    "test": "ts-mocha --timeout 10000 -p tsconfig.json spec/**/*.spec.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/expect": "^24.3.0",
    "@types/mocha": "^10.0.1",
    "@types/node": "^20.3.1",
    "chai": "^4.3.7",
    "prettier": "^2.8.8",
    "ts-mocha": "^10.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "node-fetch": "^3.3.1",
    "pyodide": "^0.23.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
